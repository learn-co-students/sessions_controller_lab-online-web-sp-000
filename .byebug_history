exit
session[:name]
!!session[:name]
!!session[:name}
if session[:name]
        redirect_to root_path
    else
      if params[:name] && params[:name].present?
          session[:name] = params[:name] 
      else
          redirect_to login_path
      end
    end
params[:name] = 'chet'
if session[:name]
        redirect_to root_path
    else
      if params[:name] && params[:name].present?
          session[:name] = params[:name] 
      else
          redirect_to login_path
      end
    end
session[:name] ? puts 'yes'; puts 'no'; end
session[:name] ? puts 'yes'; else  puts 'no'; end
session[:name] ? puts 'yes' : puts 'no'
if session[:name]; end
if session[:name]
